CREATE TABLE IF NOT EXISTS cumro_users(userId INT PRIMARY KEY AUTO_INCREMENT, username VARCHAR(60)UNIQUE,password VARCHAR(60));
CREATE TABLE IF NOT EXISTS cumro_books(bookId INT PRIMARY KEY AUTO_INCREMENT, title VARCHAR(60), author VARCHAR(60), pageNum INT, finished BOOLEAN, userID INT, FOREIGN  KEY (userID) REFERENCES cumro_users (userId));